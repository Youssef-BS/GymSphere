<!DOCTYPE html>
<html lang="en">
<head>
   <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
        }

        .container-wrapper {
            margin-left: 250px; /* Adjust as needed */
            margin-top: 50px; /* Adjust as needed */
        }

        .container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            padding: 20px;
        }

        .user-container {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin: 20px;
            padding: 20px;
            width: 280px;
            text-align: center;
            transition: transform 0.3s ease;
            
        }

        .user-container:hover {
            transform: translateY(-5px);
        }

        .user-container img {
            border-radius: 50%;
            width: 150px;
            height: 150px;
            object-fit: cover;
            margin-bottom: 20px;
        }

        .user-info h1 {
            margin: 0;
            font-size: 24px;
            color: #333;
        }

        .user-info p {
            margin: 5px 0;
            color: #666;
        }

        .consulter a {
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 4px;
            padding: 8px 16px;
            margin: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            text-decoration: none; /* Ensure link text decoration is removed */
            display: inline-block; /* Ensure link behaves like a block element */
        }

        .consulter a:hover {
            background-color: #0056b3;
        }

        button {
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 4px;
            padding: 8px 16px;
            margin: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #0056b3;
        }
        .consulter-user {
         position : absolute;
         right : 4%;
         top : 3%;
         width : 200px ;
         top: 8px;
          background-color: #007bff;
            color: white;
            border: none;
            border-radius: 4px;
            padding: 8px 16px;
            margin: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            text-align: center;
            
        }
    </style>
</head>
<body>
{% include 'admin/component/sideBar.html.twig' %}
 <div class="consulter-user">
    <a href="{{ path('addUserPage') }}">Add User</a>
    </div>
<div class="container-wrapper">
    <div class="container"> 
   
        {% for user in users %}
        <div class="user-container">
            <img src="https://wac-cdn.atlassian.com/fr/dam/jcr:ba03a215-2f45-40f5-8540-b2015223c918/Max-R_Headshot%20(1).jpg?cdnVersion=1504" alt="" />
            <div class="user-info">
                <h1>{{ user.nom }}</h1>
                <p>{{ user.isAdmin ? 'Admin' : 'User' }}</p>
            </div>
            <div class="consulter">
               <form method="post" action="{{ path('deleteUser', {'id': user.idUser}) }}" onsubmit="return confirm('Are you sure you want to delete this user?')">
               <a href="{{ path('userDetail', {'id': user.idUser}) }}">consulter</a>
               <a href="{{ path('deleteUser', {'id': user.idUser}) }}" onclick="return confirm('Are you sure you want to delete this user?')">Delete</a>

</form>
            </div>
        </div>
        {% endfor %}
    </div>
</div>
</body>
</html>
