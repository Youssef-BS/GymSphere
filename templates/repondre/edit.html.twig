{% extends 'base.html.twig' %}

{% block title %}Modifier Réponse{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mt-4 mb-4">Modifier Réponse</h1>

        <div class="card mb-4">
            <div class="card-body">
                {{ include('repondre/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

                <a href="{{ path('app_repondre_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>

                {{ include('repondre/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
